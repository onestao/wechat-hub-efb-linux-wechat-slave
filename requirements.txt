# Python dependencies with pinned versions for reproducible CI builds.
# Hashes are omitted because the Docker build resolves the Kettly EFB master
# fork from a fixed Git revision; adding hashes here would break that path.
ehforwarderbot>=2.0.0,<3
requests>=2.28,<3
PyYAML>=6.0,<7
